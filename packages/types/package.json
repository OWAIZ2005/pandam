{
  "name": "@pandam/types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types for PANDAM (frontend + backend)",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint \"src/**/*.ts\"",
    "typecheck": "tsc --noEmit",
    "test": "echo \"@pandam/types: no tests yet\"",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "devDependencies": {
    "@pandam/config": "workspace:*",
    "eslint": "^8.57.1",
    "typescript": "^6.0.3"
  }
}
