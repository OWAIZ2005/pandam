/**
 * @pandam/ui — cross-platform UI foundation.
 *
 * Only the theme tokens and one themed primitive (`Text`) exist today. The full
 * component library and design system are built out later, on this base.
 */
export * from './tokens';
export { Text, type TextProps } from './components/Text';
