/**
 * @pandam/utils — small, framework-agnostic helpers shared across the platform.
 * Only add functions here that are genuinely used in more than one package.
 */
export * from './result';
export * from './time';
