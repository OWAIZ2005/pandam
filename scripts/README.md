# scripts

| Script     | Purpose                                                                                         |
| ---------- | ----------------------------------------------------------------------------------------------- |
| `check.sh` | Runs the full local quality gate (format, lint, typecheck, test, build) — the same steps as CI. |
